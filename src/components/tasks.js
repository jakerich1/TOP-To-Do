export default function tasks() {

    //Main Projects container
    const tasks = document.createElement('div')
    tasks.classList.add('task-area')

    tasks.innerHTML = "test"

    return tasks

}